@model Costify.Domain.Entities.Recipe
@{
    ViewData["Title"] = Model.Name;
    var fcpct = Model.FoodCostPercentage;
    var fcColor = fcpct < 25 ? "#10b981" : fcpct < 35 ? "#f59e0b" : "#ef4444";
    var fcLabel = fcpct < 25 ? Localizer["Recipes_Ideal"].Value : fcpct < 35 ? Localizer["Recipes_Moderate"].Value : Localizer["Recipes_High"].Value;
}

<div class="row g-3">
    <div class="col-lg-4">
        <div class="card">
            <div class="card-header">@Localizer["Recipes_Info"]</div>
            <div class="card-body">
                <dl class="row mb-0 small">
                    <dt class="col-6 text-muted">@Localizer["Recipes_RecipeName"]</dt>
                    <dd class="col-6 fw-semibold">@Model.Name</dd>

                    @if (!string.IsNullOrEmpty(Model.Category))
                    {
                        <dt class="col-6 text-muted">@Localizer["Common_Category"]</dt>
                        <dd class="col-6">@Model.Category</dd>
                    }

                    <dt class="col-6 text-muted">@Localizer["Recipes_SellingPrice"]</dt>
                    <dd class="col-6 fw-bold fs-6">@Model.SellingPrice.ToString("N2") ₺</dd>

                    <dt class="col-6 text-muted">@Localizer["Recipes_IngredientCost"]</dt>
                    <dd class="col-6 fw-bold">@Model.TotalCost.ToString("N2") ₺</dd>

                    <dt class="col-6 text-muted">@Localizer["Recipes_GrossProfit"]</dt>
                    <dd class="col-6 fw-bold text-success">@((Model.SellingPrice - Model.TotalCost).ToString("N2")) ₺</dd>

                    <dt class="col-6 text-muted">@Localizer["Common_Status"]</dt>
                    <dd class="col-6">
                        <span class="badge @(Model.IsActive ? "bg-success-soft" : "bg-secondary-soft")">
                            @(Model.IsActive ? Localizer["Common_Active"].Value : Localizer["Common_Inactive"].Value)
                        </span>
                    </dd>
                </dl>

                @if (!string.IsNullOrEmpty(Model.Description))
                {
                    <div class="mt-3 p-2 bg-light rounded small text-muted">
                        <i class="bi bi-chat-text me-1"></i>@Model.Description
                    </div>
                }

                <div class="mt-4 p-3 rounded-3 text-center" style="background:#f8fafc;border:2px solid @fcColor">
                    <div class="text-muted small mb-1">@Localizer["Recipes_FoodCostPercentage"]</div>
                    <div class="fw-bold" style="font-size:2rem;color:@fcColor">%@fcpct.ToString("N1")</div>
                    <span class="badge" style="background:rgba(0,0,0,.06);color:@fcColor;border:1px solid @fcColor">@fcLabel</span>
                    <div class="mt-2" style="font-size:.75rem;color:#64748b">
                        @if (fcpct < 25) { <span><i class="bi bi-check-circle me-1"></i>@Localizer["Recipes_TargetExcellent"]</span> }
                        else if (fcpct < 35) { <span><i class="bi bi-info-circle me-1"></i>@Localizer["Recipes_TargetBring"]</span> }
                        else { <span><i class="bi bi-exclamation-triangle me-1"></i>@Localizer["Recipes_ReduceCost"]</span> }
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <a asp-action="Index" class="btn btn-sm btn-outline-secondary">@Localizer["Common_Back"]</a>
            </div>
        </div>
    </div>

    <div class="col-lg-8">
        <div class="card">
            <div class="card-header">@Localizer["Recipes_IngredientList"]</div>
            <div class="card-body p-0">
                <table class="table mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>@Localizer["Recipes_ProductRawMaterial"]</th>
                            <th class="text-center">@Localizer["Common_Quantity"]</th>
                            <th class="text-end">@Localizer["Common_UnitPrice"]</th>
                            <th class="text-end">@Localizer["Recipes_LineCost"]</th>
                            <th class="text-end">@Localizer["Recipes_CostShare"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var ing in Model.Ingredients.OrderByDescending(i => i.TotalCost))
                        {
                            var share = Model.TotalCost > 0 ? (ing.TotalCost / Model.TotalCost * 100) : 0;
                            <tr>
                                <td>@ing.Product?.Name</td>
                                <td class="text-center">
                                    @ing.Quantity.ToString("N4")
                                    <small class="text-muted">@ing.Product?.Unit?.Symbol</small>
                                </td>
                                <td class="text-end text-muted" style="font-size:.85rem">
                                    @(ing.Product?.LastPurchasePrice.ToString("N2") ?? "—") ₺
                                </td>
                                <td class="text-end fw-semibold">@ing.TotalCost.ToString("N2") ₺</td>
                                <td class="text-end" style="font-size:.85rem">
                                    <div class="d-flex align-items-center justify-content-end gap-2">
                                        <div style="width:60px;height:6px;background:#e2e8f0;border-radius:3px;overflow:hidden">
                                            <div style="width:@share.ToString("0")%;height:100%;background:var(--primary);border-radius:3px"></div>
                                        </div>
                                        <span>%@share.ToString("N0")</span>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                    <tfoot class="table-light">
                        <tr>
                            <td colspan="3" class="text-end fw-bold">@Localizer["Recipes_TotalCostLabel"]</td>
                            <td class="text-end fw-bold fs-6">@Model.TotalCost.ToString("N2") ₺</td>
                            <td></td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
